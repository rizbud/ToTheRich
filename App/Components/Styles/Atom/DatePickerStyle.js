import {connect} from '@Themes/OsmiProvider';

export default connect({
  container:
    'border border-gray full h-50 p-2 rounded-md font-regular row items-center justify-between mb-5',
  text: 'font-regular',
});
