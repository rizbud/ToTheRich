import {connect} from '@Themes/OsmiProvider';

export default connect({
  container:
    'border border-gray full h-50 px-2 rounded-md row items-center mb-5',
  prefix: 'font-regular',
  input: 'font-regular flex',
});
