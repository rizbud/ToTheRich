import {connect} from '@Themes/OsmiProvider';

export default connect({
  container:
    'border border-gray full h-50 p-2 rounded-md font-regular row items-center justify-between',
  placeholder: 'text-gray-3',
  circle: 'w-24 h-24 items-center justify-center rounded-full bg-gray',
});
